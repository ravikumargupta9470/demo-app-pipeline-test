apiVersion: v1
kind: Service
metadata:
  name: springboot-service-demo-app-test
spec:
  type: NodePort
  selector:
    app: springboot
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30009
